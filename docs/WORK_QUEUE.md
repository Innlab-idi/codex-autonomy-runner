# Work queue

Operational coordination only. Requirements, architecture, and governance live in their canonical documents.

| Checkpoint | Gate | State | Scope | Dependency / next action |
| --- | --- | --- | --- | --- |
| BOOT-01 | AI | DONE | Establish the public baseline documentary and governance contract. | AI SUPERVISOR approved substantive HEAD `f70191b7d1e44af83d831f6462a877c4130dcada`; closure materialized. |
| ARCH-01 | AI | DONE | Assess implementation architecture and prototype lessons; recommend a portable runtime design without implementing it. | AI SUPERVISOR approved substantive HEAD `54f7f6b529165b58cdc2d6ee71a3cadf896b1a79`; closure materialized. |
| ARCH-02 | HUMAN | DONE | Materialize the HUMAN OWNER-approved implementation architecture in canonical documentation. | HUMAN OWNER approved the architecture in durable issue #3 and explicitly authorized closure/merge after AI SUPERVISOR review of substantive HEAD `0c4ef4d2ca333ec383471a12ce96c15dbaf07587`; closure materialized. |
| ARCH-03 | HUMAN | DONE | Materialize the HUMAN OWNER-approved actor ownership; AI COORDINATOR/SUPERVISOR separation; strict CODEX WORKER/HOST privilege separation; deterministic FINALIZER/RUNNER boundaries; future versioned actor instructions; runtime sequencing/timing principles; and separation from future multi-repo orchestration. | AI SUPERVISOR approved substantive HEAD `f7215574bdf346b86f26b99cdaf7dede80aee9be`; HUMAN OWNER explicitly authorized closure/merge in PR #8; closure materialized. |
| CORE-01 | AI | DONE | Implement only a deterministic Python foundation for native-process execution: structured argv and explicit cwd; explicit UTF-8 boundaries; structured exit code, stdout, and stderr capture; launch-failure distinction; unit tests for success, non-zero exit, stdout/stderr, spaced/quoted arguments, non-ASCII text, cwd, and launch failure. Excludes Git/GitHub, governance/queue parsing, worker/adapter, consumer configuration, locking, recovery, persistent observability, finalizer/merges, scheduler/launcher, multi-repo orchestration, packaging, Python/platform version decisions, and consumer-specific rules. | AI SUPERVISOR approved substantive HEAD `978860549628df5e406e7c2bdc324687400d88a7`; closure materialized. |
| CORE-02 | AI | DONE | Implement only deterministic read-only Git/repository inspection on top of CORE-01: resolve repository root, current HEAD, branch/detached state, and a stable changed-path inventory including tracked/index/worktree changes and untracked paths; expose small structured Python results and focused tests using temporary repositories. Excludes network/GitHub, any repository mutation, governance/queue parsing, worker/adapter, consumer configuration, locking, recovery, persistent observability, finalizer/merges, scheduler/launcher, multi-repo orchestration, packaging, Python/platform version decisions, and consumer-specific rules. | AI SUPERVISOR approved substantive HEAD `c4849655790056fcbacb8245e8e6f8695a12b4c1`; closure materialized. |
| CORE-03 | AI | DONE | Define only the minimal internal Python invocation/result model required by the future runtime: an immutable invocation request for one selected repository and intended ref, plus structured operational outcomes that distinguish completed authorized transition, `NO_OP`, durably supported `BLOCKED`, runtime/execution failure, and interrupted/untrustworthy execution without prose parsing. Excludes serialization or wire formats, governance/queue parsing or selection, GitHub/network, repository mutation, worker/adapter execution, HOST publication, FINALIZER behavior, locking, recovery persistence, observability/logging, scheduler/launcher, multi-repo orchestration, packaging, supported-version policy, and consumer-specific rules. | AI SUPERVISOR approved substantive HEAD `692980750e89666bd8f1e151c106323edeb2593b`; closure materialized. |
| CORE-04 | AI | DONE | Implement only a deterministic read-only existing-work reconciliation model over caller-supplied fresh durable observations: immutable PR/work observations with explicit checkpoint identity and exact branch/HEAD identity, plus a pure discovery result that distinguishes no matching active work, exactly one matching active unit, and ambiguous multiple matches for one already-selected checkpoint. Excludes live GitHub/network access or choice of GitHub client mechanism, parsing PR prose/comments, governance/queue parsing or checkpoint selection, branch-name derivation or creation, repository mutation, supervisor judgment, FINALIZER/HOST/worker behavior, locking, recovery, observability, scheduler, multi-repo orchestration, packaging, and consumer-specific rules. | AI SUPERVISOR approved substantive HEAD `81701b0cae60d5645534dcee8a3eedd546ded92c`; closure materialized. |
| CORE-05 | AI | DONE | Implement only a deterministic read-only supervisor-decision reconciliation model over caller-supplied durable decision observations for one already-selected checkpoint/PR: immutable observations with explicit decision kind and exact reviewed substantive HEAD identity, plus a pure result that distinguishes no applicable decision, exactly one current applicable decision, and ambiguous/conflicting applicable decisions without semantic invention. Excludes live GitHub/network access or choice of GitHub client mechanism, parsing comments/prose, governance/queue parsing or checkpoint selection, deciding whether review evidence is sufficient, publishing supervisor decisions, FINALIZER/HOST/worker behavior, repository mutation, locking, recovery, observability, scheduler, multi-repo orchestration, packaging, and consumer-specific rules. | AI SUPERVISOR approved substantive HEAD `5edb9ab5b6cd2bd701b2ae50dfab67dc4c71e4a9`; closure materialized. |
| CORE-06 | AI | DONE | Implement only deterministic pure changed-path allowlist validation over caller-supplied CORE-02 changed-path inventory and caller-supplied exact permitted repository-relative paths: derive a stable actual-path set including staged, unstaged, and untracked paths, partition it into permitted and unexpected paths, and expose a structured immutable validation result without performing side effects or policy inference. Matching must be exact path identity with no glob, prefix, directory, case-folding, or normalization semantics. Excludes filesystem/Git I/O, staging/commit/push, deriving the allowlist, governance/queue parsing or checkpoint selection, GitHub/network, HOST publication, worker/finalizer behavior, locking, recovery, observability, scheduler, multi-repo orchestration, packaging, and consumer-specific rules. | AI SUPERVISOR approved substantive HEAD `8b4fb5e3482849c5b648c9bbaec77296f9621b82`; closure materialized. |
| CORE-07 | AI | DONE | Implement only deterministic pure worker-boundary validation over caller-supplied pre-worker and post-worker `RepositoryInspection` snapshots plus caller-supplied CORE-06 changed-path validation: verify the same repository root, exact HEAD unchanged, branch/detached identity unchanged, a clean pre-worker change inventory, and a valid post-worker exact-path allowlist result; expose an immutable structured result with explicit mechanical violations and no side effects or semantic inference. Excludes obtaining the expected HEAD, filesystem/Git I/O, worker invocation, allowlist derivation, governance/queue parsing or selection, staging/commit/push, GitHub/network, HOST publication, FINALIZER behavior, locking, recovery, observability, scheduler, multi-repo orchestration, packaging, and consumer-specific rules. | AI SUPERVISOR approved substantive HEAD `c92be5f8bf9a28af9d40432d677100147aaec412`; closure materialized. |
| CORE-08 | AI | DONE | Implement only deterministic pure execution-baseline resolution over caller-supplied fresh durable intended-ref observation and CORE-04 existing-work discovery for one already-selected checkpoint: if unique active work exists, preserve its exact PR/branch/HEAD; if none exists, freeze the exact observed intended-ref HEAD as the new-work baseline; ambiguous existing work stays unresolved. Excludes live Git/GitHub/network I/O, branch creation/checkout, deriving branch names, queue/governance parsing or checkpoint selection, worker invocation, repository mutation, HOST publication, finalization, locking, recovery, observability, scheduler, multi-repo orchestration, packaging, and consumer-specific rules. | AI SUPERVISOR approved substantive HEAD `807b358309703edc060169e84706b913882175cf`; closure materialized. |
| CORE-09 | AI | DONE | Implement only deterministic pure pre-worker preparation validation over a caller-supplied CORE-08 execution-baseline resolution and CORE-02 `RepositoryInspection`: require a resolved baseline, a clean prepared working tree, and the exact expected HEAD; for existing work additionally require the exact reconciled branch and non-detached state; for new work require only a non-detached prepared branch without deriving or naming it. Excludes repository/path resolution, Git/filesystem/GitHub I/O, branch creation/checkout/naming, worker invocation, allowlist derivation, HOST publication, finalization, locking, recovery, observability, scheduler, multi-repo orchestration, packaging, and consumer-specific rules. | AI SUPERVISOR approved substantive HEAD `0269f8cb6684417551a6ec4dcb5711a6fa6b8750`; closure materialized. |
| ARCH-04 | HUMAN | DONE | Materialize only the HUMAN OWNER-approved adaptive-roadmap authority contract: roadmap reconciliation by AI COORDINATOR may conservatively restructure future phases from durable evidence and approved decisions without crossing HUMAN-reserved choices; only the next currently valid phase may be decomposed just-in-time into executable checkpoints through an explicitly authorized planning transition; active/materialized checkpoint contracts may not be silently rewritten. Preserve RUNNER as mechanical and consumer-local ownership of roadmap/work queue/evidence/delegation. | AI SUPERVISOR approved substantive HEAD `6c922b0c10edddebb08bcbef65c5c921ff5ca1c4`; HUMAN OWNER authorized closure/merge in PR #26, with that authorization durably persisted by AI COORDINATOR from the owner's explicit conversation instruction; closure materialized. |
| PLAN-FINALIZER-01 | AI | DONE | Reconcile the completed CORE and current durable finalizer policy, then decompose only the next valid `FINALIZER-*` phase into the minimum executable checkpoints with dependencies, Gate AI/HUMAN, evidence requirements, done criteria, scope limits, and ordering. Planning only: do not implement FINALIZER, HOST, RUNNER, Git/GitHub mechanics, or any later roadmap phase. | AI SUPERVISOR approved substantive HEAD `b6f47039d3947b6cbf19b9c40ed110a5967ac353`; closure materialized. |
| FINALIZER-01 | AI | DONE | Implement only pure, deterministic finalizer-eligibility reconciliation over caller-supplied fresh durable observations: bind repository/PR/checkpoint/Gate/state, substantive HEAD, the exact current AI SUPERVISOR decision, invalidation status, checks, mergeability, and HUMAN-reserved condition; determine whether an already-approved unit is eligible and whether an allowlisted closure is required. No Git/GitHub I/O or mutation. | AI SUPERVISOR approved substantive HEAD `de648bf19723fa8246b3e78d0595b353b6251651`; closure materialized. |
| FINALIZER-02 | AI | DONE | Implement only pure, deterministic derivation and validation of an allowlisted closure for an eligible Gate-AI unit requiring closure: represent a reproducible closure; permit by default only operational `AI_REVIEW -> DONE` metadata in `docs/WORK_QUEUE.md`; validate exact path/content restrictions and reject semantic changes. Reuse CORE-06 for an observed diff when supplied. No commit, push, PR update, merge, durable refresh, or general HOST publication. | AI SUPERVISOR approved substantive HEAD c2db7af28b80a4a6f5605fafbdc0d4841b55160d; closure materialized. |
| FINALIZER-03 | AI | DONE | Consume a caller-supplied fresh eligible unit and valid `FINALIZER-02` closure plan through a narrow finalizer-specific Git seam; apply the closure only to the approved substantive expected HEAD, create and non-force publish the closure commit, and return the exact observed post-publication HEAD for revalidation. Reuse CORE-01 for structured commands and CORE-02/CORE-06 for applicable validation. Does not decide eligibility, approve, merge, create/update ordinary PRs, prepare worker branches, implement locks, refresh scheduling, or general HOST publication, or select a general GitHub library/client. | AI SUPERVISOR approved substantive HEAD 96d2b84c9abf5dba7bd636dc9bbccd932cd5c4ba; closure materialized. |
| FINALIZER-04 | AI | DONE | Consume caller-supplied fresh eligibility/revalidation; for a closure path require post-closure revalidation through FINALIZER-01 and support its direct-merge path. Act only on the exact approved/current HEAD through a narrow finalizer-specific protected-merge transport seam with `expected_head_sha` or equivalent, returning a structured result without mapping operational failure to `BLOCKED`. Does not approve, select work, invoke a worker, perform ordinary HOST publication, or implement locks, retry/recovery, logging, scheduler, or general durable refresh. | AI SUPERVISOR approved substantive HEAD `995172ef06aee710c6bf9b51a415eb89623c2b95`; PR #35 completed the manual bootstrap merge with exact expected-HEAD protection; closure state repaired. |
| PLAN-RUNTIME-01 | AI | DONE | Planning only: reconcile durable state after FINALIZER; consider adopted architectural/authority decisions and D-013 check-execution boundaries; decompose just-in-time only `RUNTIME-*` into the minimum executable checkpoints, each with dependencies, AI/HUMAN Gate, required evidence/checks, done criteria, scope limits, and minimum order. Keep PILOT/ADOPT undecomposed, MULTI-REPO ORCHESTRATOR outside the runner, and issue #39 only as a future non-adopted study not made architecturally impossible. Does not implement RUNTIME. | AI SUPERVISOR approved substantive HEAD `cf936d15c7f2e2e7ec60f5cff73a896e0bbea91c`; PR #41 merged that plan; operational closure repair only, with no RUNTIME plan change or new authority. |
| RUNTIME-01 | AI | DONE | Implement only the repository-local mechanical invocation foundation: per-repository mutual exclusion, technical preflight, structured lifecycle/result boundary, interruption/uncertainty capture, and non-sensitive local operational evidence. Reuse CORE-01/02/03. No durable GitHub acquisition, FINALIZER invocation, worker invocation, HOST publication, scheduler, consumer-policy parsing, or configuration/model policy. | AI SUPERVISOR approved substantive HEAD `395dd5574c1546ac909e02c564f46fcb67d1de26`; closure materialized. |
| RUNTIME-02 | AI | DONE | Implement only host-side acquisition of fresh repository/GitHub facts and explicit adaptation into CORE-04 existing-work observations, CORE-05 supervisor-decision observations, and FINALIZER-01 eligibility observations; supply only the narrow real transports required by FINALIZER-03 closure publication and FINALIZER-04 expected-HEAD protected merge; execute FINALIZER only as a pre-pass and require fresh durable refresh/reconciliation after any successful durable change. No checkpoint selection, queue/roadmap interpretation, approval, ordinary PR publication, worker branch preparation/invocation, consumer checks, or reusable general GitHub client. | AI SUPERVISOR approved substantive HEAD `1b5dfe33872fe2db08303434b00c8b660ac2d63f`; closure materialized. |
| RUNTIME-03 | AI | DONE | Implement only HOST preparation or continuation for one already-authorized transition, one structured CODEX WORKER invocation without GitHub credentials or `.git` write authority, post-worker CORE-02/06/07 validation, and execution of only caller-declared focused and publication checks; reuse CORE-04/06/07/08/09 and fresh RUNTIME-02 observations. No checkpoint selection or approval, consumer-policy parsing, staging/commit/push/PR publication, post-worker FINALIZER, test-sufficiency inference, model/profile policy, or scheduler. | AI SUPERVISOR approved substantive HEAD e95144139bc66d7e07541fcac5aed117fbaafb35; closure materialized. |
| RUNTIME-04 | AI | DONE | Implement only HOST publication of a fresh valid post-worker result with exact permitted paths and fresh declared publication-check satisfaction evidence: exact staging, commit, non-force push, remote HEAD verification, and mechanical create/update of the corresponding PR and durable review-ready state. Reuse existing CORE limits and RUNTIME-02 narrow transport conventions. Committed-but-unpublished, uncertain publication, and other failures remain structured recoverable/untrustworthy operational evidence as appropriate, without inventing remote state. No work selection, approval/merge, queue-semantic change, post-worker FINALIZER, check inference/retry, worker GitHub credentials, scheduler, model/profile policy, RUNTIME-05, PILOT, ADOPT, or MULTI-REPO ORCHESTRATOR. | AI SUPERVISOR approved substantive HEAD `1a12bf0f31025b84216393403a4e38ca6aa6cd50`; closure materialized. |
| RUNTIME-05 | AI | DONE | Compose the complete repository-local wake from the reviewed RUNTIME-01 through RUNTIME-04 primitives: lock and technical preflight; fresh durable refresh; FINALIZER pre-pass; mandatory fresh refresh/reconciliation after successful finalization; coordinator-supplied `NO_OP` or one already-authorized transition; HOST preparation, one worker invocation, validation/checks and publication; structured result/evidence; unlock; and bounded recovery/resumption from uncertain evidence without duplicate semantic execution. No semantic work selection, post-worker FINALIZER, scheduler/launcher, model/profile policy, PILOT, ADOPT, or multi-repository orchestration. | AI SUPERVISOR approved substantive HEAD `478796210cb78005f2f9cda1ee0a171485a1272e`; PR #51 merged that implementation; operational closure repair only, with no RUNTIME scope change or new authority. |
| PLAN-PILOT-01 | AI | AI_REVIEW | Planning only: reconciled the completed RUNTIME phase and current durable policy, and decomposed only the next valid `PILOT-*` phase into the minimum controlled end-to-end sequence. The plan defines isolated-target selection, safety boundaries, actor/Gate ownership, declared evidence, success, abort, and recovery criteria without executing or implementing a pilot. Issue #39 remains non-adopted; ADOPT remains undecomposed, `vevi-exporter` untouched, and MULTI-REPO ORCHESTRATOR external. | Documentary planning evidence is the `PILOT phase checkpoint plan` below. A material HUMAN OWNER choice is required before an external disposable consumer repository/environment can be created or designated, so only `PILOT-01` is materialized READY; later checkpoints remain plan-only until its durable resolution. |
| PILOT-01 | HUMAN | READY | Obtain and durably record the HUMAN OWNER-approved controlled pilot target/environment: a purpose-built disposable sandbox consumer, never this runner, `vevi-exporter`, a production repository, or a repository containing production secrets, data, or business-critical state. Bind its identity/visibility, authorized access, harmless predeclared pilot transition and exact permitted paths, declared consumer checks, protected-merge capability, evidence-retention location, and permitted post-pilot cleanup boundary. | Required HUMAN OWNER choice under D-002/D-003/D-010: external repository identity/visibility, consumer adoption/delegation, and its merge/access boundary cannot be silently selected by this reusable runner. No repository is created, modified, or exercised by this checkpoint's planning predecessor. |

## FINALIZER phase checkpoint plan

This is the just-in-time plan for the selected `FINALIZER-*` phase.
`FINALIZER-01`, `FINALIZER-02`, `FINALIZER-03`, and `FINALIZER-04` above are
`DONE`; the `FINALIZER-*` phase is complete.

### FINALIZER-01 — Pure eligibility reconciliation

- **Dependencies:** Approved and closed `PLAN-FINALIZER-01`; caller-supplied fresh durable observations for repository/PR/checkpoint identity, Gate/state, substantive HEAD, current supervisor-decision discovery, invalidation/substantive-change status, checks, mergeability, and HUMAN-reserved condition.
- **Gate:** AI. This applies existing D-003/D-004/D-005/D-010 rules mechanically and makes no material product, authority, or security decision.
- **Objective and CORE reuse:** Define the immutable finalizer input/result and pure eligibility validation. Reuse CORE-04 exact existing-work identity and CORE-05 exact current supervisor-decision reconciliation; require a unique `AI_SUPERVISOR: APPROVED` bound to the substantive HEAD. CORE-03 may supply the existing operational-result vocabulary to a caller, but eligibility itself remains distinct from an invocation outcome. No CORE primitive is forced into the contract when it belongs to worker/HOST preparation instead.
- **Evidence required:** Deterministic unit tests cover exact identity/HEAD binding; missing, non-approved, ambiguous, or invalidated decisions; Gate/state; substantive change; required checks; mergeability; HUMAN condition; and closure-required versus direct-merge eligibility. Inputs are fixtures only, with no network.
- **Done when:** A pure result can either reject safely with explicit mechanical reasons or expose only the exact already-approved unit and expected substantive HEAD needed by later closure/merge work.
- **Limits:** Does not fetch or parse GitHub state, choose work, approve, interpret review evidence, derive a closure, mutate Git, or map failure to `BLOCKED`.

### FINALIZER-02 — Pure allowlisted closure derivation and validation

- **Dependencies:** Reviewed `FINALIZER-01` eligibility contract and a valid closure-required eligibility result.
- **Gate:** AI. The closure rule is already delegated by D-005; this checkpoint neither changes the allowlist nor creates new authority.
- **Objective and CORE reuse:** Define a narrow, deterministic closure representation/derivation and validate that it changes only the approved checkpoint's operational `AI_REVIEW -> DONE` metadata in `docs/WORK_QUEUE.md`. Reuse CORE-06 exact changed-path validation over the CORE-02 `ChangedPaths` representation where an observed diff is supplied. The new validation must also reject semantic changes to scope, Gate, dependencies, other checkpoints, requirements, or behavior; it is not a general queue/governance parser.
- **Evidence required:** Deterministic tests cover an allowed closure and every forbidden path/content category, exact checkpoint identity, no-op/invalid inputs, and stable output. Tests use fixtures or temporary repositories only; no remote or real PR.
- **Done when:** A valid closure plan is reproducible from the eligible approved HEAD and invalid closure input exposes no publishable plan.
- **Limits:** Does not create a commit, push, update a PR, merge, refresh durable state, or implement general HOST publication.

### FINALIZER-03 — Privileged allowlisted closure publication

- **Dependencies:** Reviewed `FINALIZER-01` and `FINALIZER-02`, a caller-supplied fresh eligible unit and closure plan, and a controlled finalizer-specific Git transport seam.
- **Gate:** AI. It performs only the pre-authorized closure; it does not extend finalizer authority to ordinary worker publication.
- **Objective and CORE reuse:** Implement the narrow side-effect boundary that applies the validated closure to the approved substantive HEAD, creates and non-force publishes the closure commit, and returns its exact observed HEAD for revalidation. CORE-01 can execute structured local commands; CORE-02 and CORE-06 validate the observed repository/diff before publication. The transport is limited to finalizer closure publication and must remain replaceable; this checkpoint does not choose a repository-wide Git/GitHub library or client.
- **Evidence required:** Tests use temporary Git repositories and fake/controlled remotes or transports to prove exact-path enforcement, clean/expected-HEAD safeguards, non-force publication behavior, refusal on invalid plans, and no merge. No consumer repository or live PR is used.
- **Done when:** Only a `FINALIZER-02`-valid closure for the exact eligible unit can be published, and its resulting HEAD is available for the mandatory post-closure recheck.
- **Limits:** Does not perform the merge, create/update ordinary PRs, prepare worker branches, stage worker output, own locks/refresh scheduling, or provide a general GitHub client.

### FINALIZER-04 — Protected merge after fresh revalidation

- **Dependencies:** Reviewed `FINALIZER-01` through `FINALIZER-03`; for a closure path, caller-supplied fresh post-closure observations and a repeated `FINALIZER-01` eligibility result. A direct-merge path still requires its own fresh eligible observations.
- **Gate:** AI. It consumes only the existing Gate-AI authorization; no HUMAN decision is pending under the adopted policy.
- **Objective and CORE reuse:** Implement the finalizer-specific protected merge operation through a narrow transport seam. It must refuse unless the exact approved/closure HEAD remains eligible immediately before action, invoke a merge guarded by `expected_head_sha` or equivalent, and return a structured operational result without translating operational failure to `BLOCKED`. Reuse `FINALIZER-01` rather than reinterpreting decisions; CORE-03 is relevant only for structured caller-facing outcome vocabulary.
- **Evidence required:** Deterministic fake-transport tests prove no merge without eligibility, refusal on decision/HEAD movement or failed recheck, exact expected-HEAD forwarding, direct and closure paths, and success/failure separation. No generic auto-merge, live PR, production, or consumer repository is a test target.
- **Done when:** The protected action merges only the previously authorized exact unit after the required recheck, and all refusal/failure cases remain observable operational results.
- **Limits:** Does not approve, select semantic work, invoke a worker, perform post-worker HOST publication, implement lock/retry/recovery/logging/scheduler behavior, or fetch/sequence durable state generally.

### Phase boundary and deferred work

FINALIZER owns the eligibility/closure/merge contract above and only its narrow
privileged closure and protected-merge transports. HOST remains responsible for
worker branch preparation, post-worker validation, staging, commit, push, and
ordinary PR publication. RUNTIME remains responsible for locks, technical
preflight, durable refresh boundaries, invocation sequencing, result capture,
and observability; it supplies fresh observations but does not make finalizer
semantics. Git/GitHub mechanism selection, configuration format, supported
versions, packaging, locking, recovery, logging, scheduler behavior, and all
`RUNTIME-*`, `PILOT-*`, and `ADOPT-*` decomposition remain deferred. No new
HUMAN gate is identified: the remaining choices are bounded, reversible
technical seams under the already adopted Python/finalizer architecture.

## RUNTIME phase checkpoint plan

This is the just-in-time plan for the selected `RUNTIME-*` phase.
`RUNTIME-01` through `RUNTIME-05` above are `DONE`; the `RUNTIME-*` phase is
complete. The plan supplies repository-local mechanical capacity only. AI
COORDINATOR retains semantic reconciliation and selection, and later
`PILOT-*` and `ADOPT-*` remain at roadmap level.

### RUNTIME-01 — Repository-local invocation foundation

- **Dependencies:** Closed CORE and FINALIZER phases; a caller-supplied `CORE-03` `InvocationRequest` for one repository/ref. No GitHub observation, selected checkpoint, worker, or finalizer action is required.
- **Gate:** AI. Per-repository exclusion, technical preflight, lifecycle result capture, and local non-sensitive evidence are bounded mechanical capacity under D-009/D-010; they decide no consumer work or authority.
- **Objective and reuse of existing CORE/FINALIZER primitives:** Provide a narrow repository-local invocation envelope that acquires/releases mutual exclusion, verifies technical preconditions, uses `CORE-01` for native-process boundaries and `CORE-02` for local inspection, and returns `CORE-03` outcomes without prose parsing. It must capture interruption or uncertain completion distinctly from operational failure and retain only local, non-sensitive attempt evidence needed for later recovery; it does not interpret that evidence as checkpoint state. The boundary must accept an opaque worker-execution profile token without assigning meanings, models, escalation rules, or policy to it.
- **Evidence/checks required:** **Focused:** deterministic unit tests with temporary repositories/fake clocks or seams cover lock contention/release, preflight refusal, result classification, interruption/uncertainty, and redacted evidence. **Publication:** those focused tests plus the full runner suite once for this implementation checkpoint, never merely because an operational wake occurs. **Full suite:** required once as publication evidence; not an idle/wake check. Before `AI_REVIEW`, record the exact focused/full-suite results and relevant non-sensitive fixture evidence.
- **Done when:** One caller-directed repository invocation can enter and leave a fail-safe mechanical envelope with a structured outcome and inspectable local recovery/evidence boundary, without mutating consumer durable coordination.
- **Limits:** Does not choose repositories or checkpoints, parse governance, access GitHub, invoke FINALIZER or CODEX WORKER, create branches/PRs, publish Git changes, implement a scheduler/launcher, define persistence/logging formats, or define worker-profile configuration.

### RUNTIME-02 — Fresh durable-state and FINALIZER pre-pass integration

- **Dependencies:** Reviewed `RUNTIME-01`; fresh caller-directed repository context; FINALIZER-01 through FINALIZER-04. It may consume the invocation envelope but does not select semantic work.
- **Gate:** AI. This is a read-side durable-state adapter plus narrowly delegated finalizer transports; it applies existing identity/approval policy mechanically and creates no generic GitHub authority.
- **Objective and reuse of existing CORE/FINALIZER primitives:** Implement host-side acquisition of fresh repository/GitHub facts and explicit adaptation into `CORE-04` existing-work observations, `CORE-05` supervisor-decision observations, and `FINALIZER-01` eligibility observations. Supply only the narrow real transports required by FINALIZER-03 closure publication and FINALIZER-04 expected-HEAD protected merge; do not turn FINALIZER into a general GitHub client. Execute FINALIZER only as a pre-pass; after successful durable change, require a fresh durable refresh/reconciliation before any later worker opportunity. Failures/refusals remain structured operational outcomes under `CORE-03` and D-006.
- **Evidence/checks required:** **Focused:** deterministic fake-transport and temporary-Git tests cover fresh identity/HEAD binding, ambiguous/absent observations, adaptation fidelity, finalizer closure/direct-merge paths, expected-HEAD forwarding, and refresh-after-finalization. **Publication:** focused tests plus the full runner suite once before this checkpoint becomes review-ready; no live consumer repository/PR. **Full suite:** required once as publication evidence, not per operational pre-pass. Before `AI_REVIEW`, retain test results and fake-transport evidence for each refusal/failure/success class.
- **Done when:** The host can obtain and classify fresh durable facts for an already directed repository, invoke only an already authorized finalizer path through its narrow seams, and expose whether a mandatory fresh reconciliation is required next.
- **Limits:** Does not choose the checkpoint, interpret queue/roadmap policy, approve, create/update ordinary PRs, prepare a worker branch, invoke a worker, run consumer checks, or provide a reusable general GitHub client.

### RUNTIME-03 — HOST preparation, worker boundary, and declared checks

- **Dependencies:** Reviewed `RUNTIME-01`; an AI COORDINATOR-supplied, already authorized single semantic transition and its exact permitted paths/check declarations; fresh durable observations from `RUNTIME-02`; CORE-04/06/07/08/09. The caller, not RUNNER, supplies the selected checkpoint and worker context.
- **Gate:** AI. HOST preparation and worker containment are mechanical; they neither select work nor expand worker authority.
- **Objective and reuse of existing CORE/FINALIZER primitives:** Implement HOST continuation of the exact legitimate branch or new-work preparation from a frozen intended-ref baseline, then validate it with CORE-08/09. Invoke CODEX WORKER once through a structured context boundary without GitHub credentials or `.git` write authority; pass only resolved authorized context and preserve opaque future profile input at that boundary. Inspect before/after with CORE-02, validate exact paths with CORE-06, and enforce CORE-07. Execute only focused and publication checks declared for the selected consumer/checkpoint transition; keep the declaration/evaluation seam separate from syntax, policy inference, and scheduling.
- **Evidence/checks required:** **Focused:** temporary-repository/fake-worker tests cover new versus exact existing branches, dirty or moved preparation, one worker call, worker identity/path violations including untracked files, declared focused versus publication checks, and opaque profile propagation/auditable attempt association. **Publication:** focused tests plus the full runner suite once for this code checkpoint; consumer publication checks are executed only when declared for the selected transition and their fresh satisfaction evidence is retained. **Full suite:** required once for this checkpoint's own publication, never inferred on an idle wake or as a substitute for consumer declarations. Before `AI_REVIEW`, retain fixture evidence and the declared-check outcomes separately.
- **Done when:** Given one externally authorized transition, HOST can safely prepare or continue its exact worktree, run at most one contained worker attempt, validate all actual changes, and produce structured check evidence without publishing work.
- **Limits:** Does not select or approve a checkpoint, parse or author consumer policy, create durable supervisor decisions, stage/commit/push, create/update PRs, finalize after the worker, infer test sufficiency, define model/profile policy, or run a scheduler.

### RUNTIME-04 — HOST publication of validated worker work

- **Dependencies:** Reviewed `RUNTIME-02` and `RUNTIME-03`; a fresh valid post-worker boundary result, exact permitted paths, and fresh declared publication-check satisfaction evidence. A worker attempt that is interrupted, invalid, or lacks required evidence is not publishable.
- **Gate:** AI. This is the strictly mechanical HOST publication authority already assigned by D-010; it makes no semantic determination or Gate-HUMAN crossing.
- **Objective and reuse of existing CORE/FINALIZER primitives:** Implement exact staging, commit, non-force push and remote verification for the prepared legitimate branch, then mechanically create or update the corresponding PR and required durable review-ready coordination. Reuse CORE-01 command/result handling, CORE-02 actual repository inspection, CORE-06 exact paths, CORE-07 boundary result, and `RUNTIME-02` narrow durable transport conventions. It must report a committed-but-unpublished or otherwise uncertain state as recoverable/untrustworthy operational evidence, never fabricate GitHub metadata in a commit.
- **Evidence/checks required:** **Focused:** temporary repositories plus fake/controlled remotes/transports cover exact staging, refusal of unauthorized/dirty paths, commit and non-force push failures, remote HEAD verification, PR create/update identity binding, and interrupted states. **Publication:** focused tests plus the full runner suite once; require the selected transition's already-declared publication-check evidence before any review-ready publication. **Full suite:** required once for this checkpoint's own publication, not automatically for normal wakes. Before `AI_REVIEW`, retain remote/PR fake-transport evidence and the bound declared-check evidence.
- **Done when:** Only a valid, exactly bounded worker result with satisfied declared publication checks can become a verified remote branch and mechanically review-ready PR/durable state; every other case remains a structured non-semantic outcome.
- **Limits:** Does not choose work, approve/merge, alter queue semantics, rerun FINALIZER after worker, infer/retry consumer checks, expose worker credentials, or implement multi-repository orchestration.

### RUNTIME-05 — Complete repository-local wake composition

- **Dependencies:** Reviewed `RUNTIME-01` through `RUNTIME-04`; an AI COORDINATOR integration boundary capable of receiving fresh observations and returning at most one already-authorized transition or `NO_OP`. No semantic selection algorithm is implemented by RUNNER.
- **Gate:** AI. It composes previously bounded mechanical operations and preserves the existing consumer authority boundary; no unresolved HUMAN material choice is introduced.
- **Objective and reuse of existing CORE/FINALIZER primitives:** Compose the repository-local sequence: lock and technical preflight; fresh durable refresh; FINALIZER pre-pass; mandatory fresh refresh/reconciliation after a successful finalization; coordinator-provided `NO_OP` or one transition; HOST prepare, one worker call, validation/checks, and publication; structured `CORE-03` result/evidence; unlock. Implement bounded recovery/resumption from `RUNTIME-01`/`RUNTIME-04` uncertain evidence, without automatic semantic reruns or converting runtime failure to `BLOCKED`. Expose a scheduler/launcher-neutral result boundary and non-sensitive duration/lock/outcome observability. There is no FINALIZER post-pass.
- **Evidence/checks required:** **Focused:** controlled end-to-end fake seams prove ordering, lock release on every path, fresh refresh after finalization, at most one worker attempt, valid `NO_OP`, no FINALIZER post-pass, interruption recovery without duplicate worker execution, and outcome separation. **Publication:** focused integration tests plus the full runner suite once; selected consumer checks remain only the declarations consumed in the worker/publication path. **Full suite:** required once for this checkpoint's own publication, never every heartbeat. Before `AI_REVIEW`, retain deterministic trace fixtures showing the allowed sequence and each stop/failure path.
- **Done when:** A caller can invoke one repository-local wake that mechanically coordinates the adopted flow, returns a scheduler-neutral structured outcome with non-sensitive evidence, and never acts as AI COORDINATOR, AI SUPERVISOR, or MULTI-REPO ORCHESTRATOR.
- **Limits:** Does not parse or redesign consumer governance, choose a repository or checkpoint, invent test policy, create a scheduler/launcher or packaging contract, make a worker model/profile decision, define escalation thresholds, or plan/implement PILOT, ADOPT, or multi-repository orchestration.

### Phase boundary and future compatibility

The order is `RUNTIME-01 -> RUNTIME-02 -> RUNTIME-03 -> RUNTIME-04 ->
RUNTIME-05`; it separates local lifecycle safety, durable/finalizer transport,
worker containment/checks, privileged HOST publication, and final composition
because those have different side effects and evidence. All checkpoints are
Gate AI: no new material HUMAN choice is required by the already adopted
architecture. D-013 is applied through explicit declared-check inputs and
fresh evidence, never a wake-triggered suite inference.

Issue #39 remains a future, non-adopted study. The only compatibility retained
here is an opaque worker-execution-profile input at the invocation boundary and
an optional non-sensitive attempt association in evidence; this plan defines no
model names, selection/escalation policy, thresholds, storage, or reset rules.
PILOT and ADOPT remain undecomposed, and MULTI-REPO ORCHESTRATOR remains
outside this repository-local runner.

## PILOT phase checkpoint plan

This is the just-in-time plan for the selected `PILOT-*` phase. It validates
the reviewed repository-local wake through actual, controlled integration
boundaries before any real consumer is adopted. Because the queue has no
non-executable planned state, only the first executable checkpoint is
materialized above; `PILOT-02` and `PILOT-03` are plan-only until their
dependencies are durably satisfied.

### PILOT-01 — HUMAN-approved isolated pilot target

- **Dependencies:** `PLAN-PILOT-01` in `AI_REVIEW`; completed `RUNTIME-01` through `RUNTIME-05`; no pilot repository or environment is assumed to exist.
- **Gate:** HUMAN. The reusable runner cannot silently select or create an external consumer, its visibility, its explicit delegation, or its access/merge boundary. Those are material consumer-owner and external-service choices under D-002/D-003/D-010, not a routine technical naming choice.
- **Objective:** Designate or authorize creation of one purpose-built, disposable sandbox consumer and its non-production environment. It must be distinct from `codex-autonomy-runner` and `vevi-exporter`; contain no real secrets, production data, or business-critical state; have no production-repository connection; and use a harmless, predeclared pilot transition with exact permitted paths and declared consumer checks. Record the chosen identity/visibility, baseline, authorized actors/access, protected-merge capability, evidence-retention location, and narrowly permitted cleanup boundary durably in that consumer/environment, without placing sensitive details in this public repository.
- **Actor ownership:** HUMAN OWNER supplies the consumer adoption/delegation and external target/access decision. AI COORDINATOR may prepare a bounded checklist from the durable decision but does not choose or create the target. No CODEX WORKER, HOST, AI SUPERVISOR, FINALIZER, or RUNNER execution occurs yet.
- **Evidence/checks required:** Durable HUMAN resolution identifying the controlled target and confirming every isolation property; its adopted pilot queue/policy with one harmless Gate-AI transition, exact paths, focused/publication checks, and intended review/closure path; confirmation that no credentials or production data are supplied to the worker; and a documented non-sensitive evidence-retention/cleanup boundary. This planning checkpoint itself requires only `git diff --check` and focused documentary consistency review.
- **Done when:** A later caller can direct the runner to a specifically authorized, isolated pilot consumer without inventing consumer authority, access, checks, cleanup, or a real-product objective.
- **Limits:** Does not create a repository, credential, secret, workflow, scheduler, production connection, or pilot artifact; does not select `vevi-exporter`; does not run a wake; and does not authorize ADOPT or multi-repository work.

### PILOT-02 — Controlled wake, publication, and review evidence

- **Dependencies:** `PILOT-01` is durably resolved with a valid isolated target and declared pilot transition; fresh durable state confirms that target and its baseline; the reviewed RUNTIME implementation is available unchanged.
- **Gate:** AI. The consumer's HUMAN-approved pilot policy supplies the exact transition, paths, checks, and delegated Gate-AI closure boundary. AI actors and HOST only exercise those declared limits; no new material consumer choice is made.
- **Objective:** Exercise the controlled integration path before finalization: a clean `NO_OP` wake; representative fail-closed/refusal and runtime-failure cases; then one authorized worker transition in its own wake through AI COORDINATOR framing, repository-local refresh and FINALIZER pre-pass when applicable, one contained CODEX WORKER call, HOST exact-path validation, declared focused/publication consumer checks, non-force commit/push, remote-HEAD verification, ordinary PR creation/update, and AI SUPERVISOR review decision bound to the exact published substantive HEAD. The planned cases must demonstrate that an unexpected changed path is refused, no automatic retry follows uncertain worker or publication evidence, a runtime/tool failure remains an execution failure rather than invented `BLOCKED`, and no wake invokes a worker more than once or invokes FINALIZER after that worker.
- **Actor ownership:** AI COORDINATOR reconciles fresh durable state and supplies at most one authorized transition per wake; RUNNER performs only the mechanical sequence; CODEX WORKER modifies only the declared harmless paths; HOST validates and publishes only a valid result; AI SUPERVISOR separately reviews the real PR/diff/check evidence and publishes only a HEAD-bound decision. FINALIZER is pre-pass-only and does not approve or post-process worker output.
- **Evidence/checks required:** Retain non-sensitive, exact repository/branch/PR/HEAD identities; ordered wake traces showing `NO_OP`, pre-pass placement, one-worker limit, and absence of a post-worker FINALIZER; actual changed-path inventories including the controlled unexpected-path refusal; declared consumer focused and publication-check outcomes bound to the published HEAD; non-force push and remote-HEAD verification; PR durable review-ready state; and the AI SUPERVISOR decision reread/published against the same exact HEAD. Retain controlled runtime/uncertainty refusal evidence without retrying it. Do not run the runner's full suite merely because a pilot wake occurs; runner regression checks run only if separately declared by a later code change.
- **Done when:** The sandbox has either safely refused each designed invalid/uncertain case without semantic-state invention or duplicate execution, and has one valid, review-ready, exact-HEAD-approved Gate-AI pilot PR with all declared evidence; ambiguous evidence stops the pilot rather than being repaired by reset or retry.
- **Limits:** No production target/data/secrets, force push, destructive reset, credential expansion, hidden semantic selection, duplicate worker attempt, generic auto-merge, or finalizer post-pass. A runner defect becomes bounded future AI rework; an environment/tool failure is recorded as runtime/execution failure, not `BLOCKED`; a newly material owner choice stops at `HUMAN_REQUIRED`.

### PILOT-03 — Deterministic closure, protected merge, and fresh reconciliation

- **Dependencies:** `PILOT-02` has retained a legitimate Gate-AI PR, exact substantive HEAD, satisfied declared publication evidence, and a current `AI_SUPERVISOR: APPROVED` bound to that HEAD; fresh durable state is available immediately before every finalizer action.
- **Gate:** AI. D-004/D-005/D-010 already delegate only deterministic, exact-HEAD finalization of this approved Gate-AI pilot unit. It introduces no product, authority, or target choice.
- **Objective:** In a later wake, verify the FINALIZER pre-pass path end to end: fail closed on stale, ambiguous, invalidated, or otherwise untrustworthy approval/HEAD/check/mergeability evidence; for the valid exact case, create only an allowlisted closure when required, protect the merge with `expected_head_sha` or equivalent, merge deterministically, then perform the mandatory fresh durable refresh/reconciliation. Confirm the next wake sees the merged durable result without duplicate semantic execution and that finalizer failure/refusal never becomes semantic `BLOCKED`.
- **Actor ownership:** FINALIZER mechanically validates and closes/merges only the previously approved exact unit; HOST provides only its narrow authorized durable transports; AI COORDINATOR performs fresh reconciliation after any durable finalizer mutation; RUNNER records structured operational outcomes. AI SUPERVISOR does not retarget or republish approval, and CODEX WORKER is not invoked for finalization.
- **Evidence/checks required:** Retain the pre-finalization fresh repository/PR/checkpoint/Gate/HEAD/decision/check/mergeability observations; refusal traces for stale or invalid evidence; if closure is needed, its complete allowlisted diff and resulting verified HEAD; expected-HEAD protected merge request/result; durable merged state; mandatory post-mutation refresh/reconciliation trace; and a following `NO_OP` or other fresh result proving no duplicate semantic execution. FINALIZER consumes the already-fresh declared publication-check evidence and does not rerun suites solely to finalize.
- **Done when:** The controlled consumer completes the intended lifecycle and a subsequent fresh wake reconciles it correctly, with no authority violation, unexpected path, unverified publication, stale-head approval/finalization, unsafe recovery, duplicate worker execution, or runtime failure represented as semantic `BLOCKED`. This is evidence sufficient to proceed later to `PLAN-ADOPT-*`, not authorization to materialize ADOPT now.
- **Limits:** No auto-merge, force push, destructive reset, automatic retry after uncertainty, new worker call, consumer-policy rewrite, ADOPT checkpoint, production repository, or multi-repository orchestration. A fail-closed invalid-evidence result is successful safety evidence when the refusal is as declared; a runner defect is future bounded AI rework, a tool/environment failure remains runtime/execution failure, and a material unresolved owner choice is `HUMAN_REQUIRED`.

### Phase boundary and pilot disposition

`PILOT-01 -> PILOT-02 -> PILOT-03` is the minimum sequence that keeps the
external consumer/adoption choice, controlled worker/publication/review path,
and later deterministic finalization/fresh reconciliation at their distinct
authority and evidence boundaries. Pilot success is stronger than passing
tests: it requires the retained controlled end-to-end durable evidence stated
above. Evidence ambiguity is an abort/stop condition; retained audit evidence
is not automatically deleted. After successful completion, only the
HUMAN-approved disposable target's narrowly recorded cleanup may occur, and it
must preserve the required audit evidence.

ADOPT remains undecomposed; `vevi-exporter` remains untouched. The
MULTI-REPO ORCHESTRATOR remains a separate future component outside this
repository-local runner. Issue #39 remains non-adopted: PILOT can validate the
opaque existing worker-profile seam without selecting a model/profile,
escalation threshold, reset policy, or implementation. No material reason to
revisit that study is established by this controlled plan. PILOT validates the
existing runner and its already delegated authority; it does not expand either.
